export const FIRST_JAR_SIZE = 8;
export const DEFAULT_JAR_SIZE = 15;
export const DEFAULT_REWARD_LABEL = 'bubble tea';
export const MAX_CUSTOM_CATEGORIES = 3;
export const QUIET_DAYS_BANNER = 7;
export const RECENT_FEED_LIMIT = 5;
// Phase 1: jar-only progress — no heatmap (see DECISIONS.md)

export const PRESET_CATEGORY_LABELS = [
  'Work',
  'Health',
  'Relationships',
  'Creative',
  'Personal Growth',
  'Small Joys',
] as const;

export const BANNER_MESSAGES = [
  'A small win still counts — what went well lately?',
  'Your jar is waiting. One line is enough.',
  'No pressure — whenever you are ready, we are here.',
  'Gentle reminder: you have done hard things before.',
  'Even a quiet day can hold one good moment.',
] as const;
